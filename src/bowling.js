
class BowlingGame {
    constructor(players, rolls, img, pins) {
    this.players = 0;
    this.rolls = [];
    this.img = 10;
    this.pins = 0;

    }

    /*isplayers(){
        this.player++;
        if (this.player <=0)
        this.player = false;
        resturn 'we shall have at least one player'
    }*/


    /*roll(pins) {
      this.rolls.push(pins);
    }
    score() {
      return this.rolls.reduce((roll, total) => total + roll);
    }*/

  }
  
  module.exports = BowlingGame;
